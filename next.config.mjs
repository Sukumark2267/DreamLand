/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],
        domains: ['cdn.easyfrontend.com'],
      },
};

export default nextConfig;

